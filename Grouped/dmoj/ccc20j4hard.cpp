#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

#define endl '\n'
#define ms(a,x) memset(a,x,sizeof(a))
#define SZ(x) int(x.size())

const int MAXN=

ll hs(char a, int x, ll i) {
	return c*pow[x][i];
}

int main() {
	cin.sync_with_stdio(0);
	cin.tie(0);

	string A,B; cin>>A>>B;
	ll y=0;
	for (int i=0; i<SZ(B); i++) y=(y+hs(B[i],0,i))%md;

}