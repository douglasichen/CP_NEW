#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
#define ll long long
#define ms(a,x) memset(a,x,sizeof(a))
#define SZ(x) x.size()

const int MAXN=100001;
ll dp[MAXN];

int main() {
    cin.sync_with_stdio(0);
    cin.tie(0);

	ms(dp,0);
	dp[1]=1;


}